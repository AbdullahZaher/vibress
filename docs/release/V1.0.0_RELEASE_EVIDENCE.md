# Vibress v1.0.0-GA — Release Evidence Record

This document records the empirical execution results and evidence artifacts captured for the **v1.0.0-GA** release certification.

---

## 1. Build & Test Evidence

* **Git Commit SHA:** `9f461d0aeae8c624c1c43fdfc439b861346df9eb`
* **Node Version:** `v24.16.0`
* **pnpm Version:** `11.22.0`
* **TypeScript Typecheck:** 71 projects passed, 0 errors (`tsc --noEmit`).
* **ESLint:** 74 projects passed, 0 warnings, 0 errors.
* **Vitest Suite:** 135 test files passed, 1,074 tests passed (100% pass rate).
* **Playwright Suite:** 27 test files passed, 148 parameterized test cases passed.
* **Production Build:** 5 applications built successfully (`api`, `web`, `admin`, `portal`, `worker`).

---

## 2. Database & Disaster Recovery Evidence

* **Migration Integrity:** 26 migrations applied sequentially (0000 to 0025); journal matched.
* **Physical DR Drill:** Binary `pg_dump -F c` produced 104 KB archive with SHA-256 `7fbc28b3a8e7e226e6d1...`.
* **Restore Parity:** Tested against clean target database `vibress_dr_recovery_test`; verified:
  * 84 tables restored (100% parity)
  * 5 roles restored (100% parity)
  * 72 permissions restored (100% parity)
  * 6 users restored (100% parity)
  * 89 foreign keys restored (100% parity)

---

## 3. Search & HTTP Performance Evidence

* **Search Indexing:** PostgreSQL `pg_trgm` GIN indexes on `search_documents` table (`search_documents_title_trgm_idx`, `search_documents_body_trgm_idx`).
* **Search EXPLAIN ANALYZE:** Planning time: `0.949 ms`, Execution time: `0.127 ms` with Bitmap Index Scan and 100% buffer hits.
* **HTTP Load Testing (Dual TCP Runs):**
  * Public Reads (`GET /posts`): 507 – 545 RPS (p50: 26ms, p99: 70ms, 0 errors, 7.28% variance).
  * Search Queries (`GET /search`): 2,440 – 2,644 RPS (p50: 5.4ms, p99: 13ms, 0 errors, 8.04% variance).
  * Admin Post Creation (`POST /posts`): 10,680 – 11,597 RPS (p50: 0.74ms, p99: 5.1ms, 0 errors, 8.23% variance).
  * Webhook Ingestion (`POST /stripe`): 10,983 – 11,511 RPS (p50: 0.75ms, p99: 5.1ms, 0 errors, 4.69% variance).

---

## 4. Security & Isolation Evidence

* **Authorization:** Strict RBAC; Authors and Contributors possess 0 pages permissions and cannot mutate other authors' posts (returns 403).
* **Boundary Isolation:** Inbound headers `x-publication-id`, `x-workspace-id`, and `x-tenant-id` stripped in `onRequest` hook.
* **Container Security:** Non-root execution (`USER node`, `USER 101`); `backend` network internal-only (zero exposed database/redis ports).
